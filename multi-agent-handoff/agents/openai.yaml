interface:
  display_name: "Multi-Agent Handoff"
  short_description: "Preserve, update, and transfer multi-agent handoff context"
  default_prompt: "Use $multi-agent-handoff to preserve continuity, update progress, or prepare a prompt for the next agent/session."
