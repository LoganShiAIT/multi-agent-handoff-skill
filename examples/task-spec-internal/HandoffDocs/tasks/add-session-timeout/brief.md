# Add Session Timeout Brief

## Problem
- Sessions never expire, leaving stale authenticated sessions active indefinitely.

## Goal
- Add a configurable inactivity timeout to authenticated sessions.

## Out Of Scope
- Changing the login provider or adding remember-me behavior.

## Context
- The project has no formal external spec workflow, so this task uses the internal fallback.

## Open Questions
- none
